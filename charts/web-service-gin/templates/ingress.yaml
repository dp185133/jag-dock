apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-service-gin-ingress
spec:
  rules:
  - http:
      paths:
      - pathType: Prefix
        path: /web/
        backend:
          service:
            name: web-service-gin
            port:
              number: 80

